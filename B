#include <iostream>

using namespace std;
int Count(int n) {
    if (n == 0) {
        return 0;
    }

    cout << n;
    if (n != 1) {
        cout << " ";
    }
    return Count(n - 1);
}
int main()
{
    int n;
    cin >> n;
    Count(n);

    return 0;
}
